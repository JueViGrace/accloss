package com.clo.accloss.lineasfactura.domain.mappers

import com.clo.accloss.lineasfactura.data.remote.model.LineasFacturasItem
import com.clo.accloss.lineasfactura.domain.model.LineasFactura
import com.clo.accloss.Lineas_factura as LineasFacturaEntity

fun LineasFacturaEntity.toDomain(): LineasFactura = LineasFactura(
    agencia = agencia,
    cantidad = cantidad,
    cntdevuelt = cntdevuelt,
    codcoord = codcoord,
    codhijo = codhijo,
    codigo = codigo,
    dmontoneto = dmontoneto,
    dmontototal = dmontototal,
    documento = documento,
    dpreciofin = dpreciofin,
    dpreciounit = dpreciounit,
    dvndmtototal = dvndmtototal,
    fechadoc = fechadoc,
    fechamodifi = fechamodifi,
    grupo = grupo,
    nombre = nombre,
    origen = origen,
    pid = pid,
    subgrupo = subgrupo,
    timpueprc = timpueprc,
    tipodoc = tipodoc,
    tipodocv = tipodocv,
    unidevuelt = unidevuelt,
    vendedor = vendedor,
    vndcntdevuelt = vndcntdevuelt,
    empresa = empresa,
)

fun LineasFactura.toDatabase(): LineasFacturaEntity = LineasFacturaEntity(
    agencia = agencia,
    cantidad = cantidad,
    cntdevuelt = cntdevuelt,
    codcoord = codcoord,
    codhijo = codhijo,
    codigo = codigo,
    dmontoneto = dmontoneto,
    dmontototal = dmontototal,
    documento = documento,
    dpreciofin = dpreciofin,
    dpreciounit = dpreciounit,
    dvndmtototal = dvndmtototal,
    fechadoc = fechadoc,
    fechamodifi = fechamodifi,
    grupo = grupo,
    nombre = nombre,
    origen = origen,
    pid = pid,
    subgrupo = subgrupo,
    timpueprc = timpueprc,
    tipodoc = tipodoc,
    tipodocv = tipodocv,
    unidevuelt = unidevuelt,
    vendedor = vendedor,
    vndcntdevuelt = vndcntdevuelt,
    empresa = empresa,
)

fun LineasFacturasItem.toDomain(): LineasFactura = LineasFactura(
    agencia = agencia ?: "",
    cantidad = cantidad ?: 0.0,
    cntdevuelt = cntdevuelt ?: 0.0,
    codcoord = codcoord ?: "",
    codhijo = codhijo ?: "",
    codigo = codigo ?: "",
    dmontoneto = dmontoneto ?: 0.0,
    dmontototal = dmontototal ?: 0.0,
    documento = documento ?: "",
    dpreciofin = dpreciofin ?: 0.0,
    dpreciounit = dpreciounit ?: 0.0,
    dvndmtototal = dvndmtototal ?: 0.0,
    fechadoc = fechadoc ?: "",
    fechamodifi = fechamodifi ?: "",
    grupo = grupo ?: "",
    nombre = nombre ?: "",
    origen = origen ?: 0.0,
    pid = pid ?: "",
    subgrupo = subgrupo ?: "",
    timpueprc = timpueprc ?: 0.0,
    tipodoc = tipodoc ?: "",
    tipodocv = tipodocv ?: "",
    unidevuelt = unidevuelt ?: 0.0,
    vendedor = vendedor ?: "",
    vndcntdevuelt = vndcntdevuelt ?: 0.0
)
